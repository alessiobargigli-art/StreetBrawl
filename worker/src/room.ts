import{MAX_PLAYERS,PROTOCOL_VERSION,RECONNECT_WINDOW_MS,SIMULATION_HZ,SNAPSHOT_HZ,type ClientMessage,type PlayerInput,type PlayerSlot,type ServerMessage}from'../../src/shared/protocol';import{MAX_CONTINUES_PER_PLAYER,MIN_CONTINUES_PER_PLAYER,DEFAULT_CONTINUES_PER_PLAYER}from'../../src/shared/campaign';import{AuthoritativeSimulation}from'../../src/shared/simulation';interface Env{}interface Session{socket:WebSocket;slot:PlayerSlot;token:string;nickname:string}interface ReconnectRecord{slot:PlayerSlot;token:string;nickname:string;expiresAt:number}
const CHARACTER_IDS=new Set(['alex','matt','elisa','gaga']);const isObject=(v:unknown):v is Record<string,unknown>=>typeof v==='object'&&v!==null;const isAxis=(v:unknown):v is -1|0|1=>v===-1||v===0||v===1;const isFiniteNumber=(v:unknown):v is number=>typeof v==='number'&&Number.isFinite(v);const isInput=(v:unknown):v is PlayerInput=>{if(!isObject(v))return false;return Number.isInteger(v.seq)&&isFiniteNumber(v.seq)&&isFiniteNumber(v.clientTime)&&isAxis(v.moveX)&&isAxis(v.moveY)&&typeof v.punch==='boolean'&&typeof v.kick==='boolean'&&typeof v.jump==='boolean'&&(v.continueRequestId===undefined||typeof v.continueRequestId==='string')};function parseMessage(raw:string):ClientMessage|null{let v:unknown;try{v=JSON.parse(raw)}catch{return null}if(!isObject(v)||typeof v.type!=='string')return null;switch(v.type){case'hello':return isFiniteNumber(v.protocol)&&typeof v.nickname==='string'&&(v.reconnectToken===undefined||typeof v.reconnectToken==='string')?v as unknown as ClientMessage:null;case'reserve-character':return typeof v.character==='string'&&CHARACTER_IDS.has(v.character)?v as unknown as ClientMessage:null;case'ready':return typeof v.ready==='boolean'?v as unknown as ClientMessage:null;case'settings':return isFiniteNumber(v.continuesPerPlayer)&&Number.isInteger(v.continuesPerPlayer)?v as unknown as ClientMessage:null;case'input':return Array.isArray(v.inputs)&&v.inputs.length>0&&v.inputs.length<=16&&v.inputs.every(isInput)?v as unknown as ClientMessage:null;case'pause':return typeof v.paused==='boolean'?v as unknown as ClientMessage:null;case'scene-ready':return typeof v.sceneId==='string'&&v.sceneId.length<=80?v as unknown as ClientMessage:null;case'start':return v as unknown as ClientMessage;default:return null}}
export class GameRoom{private code='';private simulation=new AuthoritativeSimulation('pending');private sessions=new Map<WebSocket,Session>();private reconnects=new Map<string,ReconnectRecord>();private tickTimer?:ReturnType<typeof setInterval>;private snapshotCounter=0;private hostSlot:PlayerSlot=0;private continuesPerPlayer=DEFAULT_CONTINUES_PER_PLAYER;constructor(private readonly ctx:DurableObjectState,private readonly env:Env){void this.ctx;void this.env}
 async fetch(request:Request){const url=new URL(request.url);if(url.pathname==='/init'&&request.method==='POST'){this.code=url.searchParams.get('code')??this.code;this.simulation=new AuthoritativeSimulation(this.code,this.continuesPerPlayer);return Response.json({ok:true})}if(request.headers.get('Upgrade')!=='websocket')return Response.json({room:this.code,players:this.simulation.state.players.length,phase:this.simulation.state.phase});const pair=new WebSocketPair(),client=pair[0],server=pair[1];server.accept();server.addEventListener('message',e=>this.onMessage(server,String(e.data)));server.addEventListener('close',()=>this.onClose(server));server.addEventListener('error',()=>this.onClose(server));return new Response(null,{status:101,webSocket:client})}
 private onMessage(socket:WebSocket,raw:string){const m=parseMessage(raw);if(!m){this.send(socket,{type:'error',code:'BAD_MESSAGE',message:'Messaggio non valido'});return}const existing=this.sessions.get(socket);if(m.type==='hello'){if(m.protocol!==PROTOCOL_VERSION){this.send(socket,{type:'error',code:'BAD_MESSAGE',message:'Versione protocollo non compatibile'});return}if(existing)return;this.pruneReconnects();let slot:PlayerSlot|undefined,token='',nickname=m.nickname.slice(0,20).trim()||'Player';const rec=m.reconnectToken?this.reconnects.get(m.reconnectToken):undefined;if(rec&&!this.isSlotActive(rec.slot)){slot=rec.slot;token=rec.token;nickname=m.nickname.slice(0,20).trim()||rec.nickname;this.reconnects.delete(rec.token)}else{const reserved=new Set([...this.reconnects.values()].map(r=>r.slot)),used=new Set([...this.sessions.values()].map(s=>s.slot));slot=([0,1]as PlayerSlot[]).find(s=>!used.has(s)&&!reserved.has(s));if(slot===undefined||this.sessions.size>=MAX_PLAYERS){this.send(socket,{type:'error',code:'ROOM_FULL',message:'Stanza piena'});socket.close(4001,'ROOM_FULL');return}token=crypto.randomUUID()}this.sessions.set(socket,{socket,slot,token,nickname});this.simulation.addPlayer(slot,nickname);if(this.sessions.size===1)this.hostSlot=slot;this.send(socket,{type:'welcome',protocol:PROTOCOL_VERSION,room:this.code,slot,reconnectToken:token});this.broadcastLobby();if(this.simulation.state.phase!=='lobby')this.send(socket,this.simulation.snapshot());this.ensureTicking();return}const session=existing;if(!session){this.send(socket,{type:'error',code:'BAD_MESSAGE',message:'Invia hello prima dei comandi'});return}
 switch(m.type){case'reserve-character':if(this.simulation.state.phase!=='lobby'){this.send(socket,{type:'error',code:'BAD_MESSAGE',message:'Personaggio selezionabile solo in lobby'});break}if(!this.simulation.reserveCharacter(session.slot,m.character))this.send(socket,{type:'error',code:'CHARACTER_TAKEN',message:'Personaggio già selezionato'});this.broadcastLobby();break;case'ready':if(this.simulation.state.phase!=='lobby'){this.send(socket,{type:'error',code:'BAD_MESSAGE',message:'Ready modificabile solo in lobby'});break}this.simulation.setReady(session.slot,m.ready);this.broadcastLobby();break;case'settings':if(session.slot!==this.hostSlot){this.send(socket,{type:'error',code:'NOT_HOST',message:'Solo host può cambiare le impostazioni'});break}if(this.simulation.state.phase!=='lobby'){this.send(socket,{type:'error',code:'BAD_MESSAGE',message:'Impostazioni modificabili solo in lobby'});break}if(m.continuesPerPlayer<MIN_CONTINUES_PER_PLAYER||m.continuesPerPlayer>MAX_CONTINUES_PER_PLAYER){this.send(socket,{type:'error',code:'BAD_MESSAGE',message:'Numero continue non valido'});break}this.continuesPerPlayer=m.continuesPerPlayer;this.simulation.setDefaultContinues(this.continuesPerPlayer);this.broadcastLobby();break;case'start':if(session.slot!==this.hostSlot){this.send(socket,{type:'error',code:'NOT_HOST',message:'Solo host può avviare'});break}if(this.simulation.state.phase!=='lobby'){this.send(socket,{type:'error',code:'BAD_MESSAGE',message:'Partita già avviata'});break}if(!this.simulation.start()){this.send(socket,{type:'error',code:'BAD_MESSAGE',message:'Tutti i giocatori devono essere pronti'});break}this.broadcast(this.simulation.snapshot());break;case'input':if(this.simulation.state.phase!=='playing')break;for(const i of m.inputs.slice(-4))this.simulation.applyInput(session.slot,i);break;case'pause':if(session.slot!==this.hostSlot){this.send(socket,{type:'error',code:'NOT_HOST',message:'Solo host può mettere in pausa'});break}if(this.simulation.state.phase==='playing'||this.simulation.state.phase==='paused'){this.simulation.state.phase=m.paused?'paused':'playing';this.broadcast(this.simulation.snapshot())}break;case'scene-ready':break}}
 private ensureTicking(){if(this.tickTimer)return;this.tickTimer=setInterval(()=>{this.pruneReconnects();this.simulation.tick();if(++this.snapshotCounter>=SIMULATION_HZ/SNAPSHOT_HZ){this.snapshotCounter=0;this.broadcast(this.simulation.snapshot())}},1000/SIMULATION_HZ)}private onClose(socket:WebSocket){const s=this.sessions.get(socket);if(!s)return;this.simulation.removePlayer(s.slot);this.sessions.delete(socket);this.reconnects.set(s.token,{slot:s.slot,token:s.token,nickname:s.nickname,expiresAt:Date.now()+RECONNECT_WINDOW_MS});if(this.simulation.state.phase==='lobby'&&s.slot===this.hostSlot)this.maybeMigrateHost(true);this.broadcastLobby();if(!this.sessions.size&&this.tickTimer){clearInterval(this.tickTimer);this.tickTimer=undefined}}private pruneReconnects(){const now=Date.now();let expired=false;for(const[t,r]of this.reconnects)if(r.expiresAt<=now){this.reconnects.delete(t);expired=true}if(expired)this.maybeMigrateHost(true)}private maybeMigrateHost(broadcast=false){if(this.isSlotActive(this.hostSlot))return;if([...this.reconnects.values()].some(r=>r.slot===this.hostSlot))return;const next=[...this.sessions.values()].sort((a,b)=>a.slot-b.slot)[0];if(!next)return;const changed=this.hostSlot!==next.slot;this.hostSlot=next.slot;if(changed&&broadcast)this.broadcastLobby()}private isSlotActive(slot:PlayerSlot){return[...this.sessions.values()].some(s=>s.slot===slot)}private broadcastLobby(){const players=this.simulation.state.players.map(({slot,nickname,character,ready,connected})=>({slot,nickname,character,ready,connected}));this.broadcast({type:'lobby',room:this.code,hostSlot:this.hostSlot,continuesPerPlayer:this.continuesPerPlayer,players})}private broadcast(message:ServerMessage){const data=JSON.stringify(message);for(const socket of this.sessions.keys())if(socket.readyState===WebSocket.OPEN)socket.send(data)}private send(socket:WebSocket,message:ServerMessage){if(socket.readyState===WebSocket.OPEN)socket.send(JSON.stringify(message))}}
